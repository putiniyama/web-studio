# Используйте официальный образ Node.js с указанной версией
FROM node:14

# Установите рабочую директорию
WORKDIR /app

# Копируйте зависимости
COPY package*.json ./

# Установите зависимости
RUN npm install

# Копируйте остальные файлы приложения
COPY . .

# Откройте порт, на котором будет работать приложение
EXPOSE 3000

# Команда для запуска сборки проекта
RUN npm run build

# Команда для запуска сервера
CMD ["npm", "start"]
